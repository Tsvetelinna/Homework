#include<iostream>
using namespace std;

void toBinary(int number)
{
	if (number >= 2)
		toBinary(number / 2);

	cout << number % 2;
}

int main()
{
	int num;
	cin >> num;
	
	 toBinary(num);

    return 0;
}

